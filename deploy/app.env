# Non-secret defaults only. The pipeline writes the deployed
# ${DEPLOY_DIR}/{dev,prod}/app.env from CI/CD variables; this file documents the
# shape and the values that are safe to keep in git.
POSTGRES_DB=portfolio
POSTGRES_USER=portfolio
PROJECT_SEED_ENABLED=true
