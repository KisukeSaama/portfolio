server:
  error:
    include-message: never
    include-stacktrace: never
  servlet:
    session:
      cookie:
        secure: true
logging:
  level:
    root: WARN
    com.jonathan.portfolio: INFO
spring:
  jpa:
    properties:
      hibernate:
        format_sql: false
