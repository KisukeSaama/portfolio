spring:
  jpa:
    hibernate:
      ddl-auto: validate
  session:
    jdbc:
      initialize-schema: never
app:
  seed:
    enabled: true
  storage:
    directory: ${java.io.tmpdir}/portfolio-media-test
