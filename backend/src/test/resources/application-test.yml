spring:
  jpa:
    hibernate:
      ddl-auto: validate
  session:
    jdbc:
      initialize-schema: never
app:
  seed:
    enabled: true
  storage:
    endpoint: http://localhost:9000
    access-key: test
    secret-key: test
    bucket: test
    public-base-url: http://localhost/media
