# Adapt with the domain and the certificate paths managed by the host.
# Le proxy applicatif reste sur 8080 ; un load balancer ou un second bloc server
# termine TLS puis transmet X-Forwarded-Proto=https.
