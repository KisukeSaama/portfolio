# Adapt with the domain and the certificate paths managed by the host.
# The application proxy stays on 8080. A load balancer or a second server block terminates TLS and
# forwards X-Forwarded-Proto=https. Adding a hop there means raising TRUSTED_PROXY_COUNT to match,
# and that hop must overwrite X-Forwarded-For rather than append to it.
